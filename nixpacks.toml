[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "libpq"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python manage.py migrate --run-syncdb && gunicorn fagni.wsgi:application --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --log-level debug"
