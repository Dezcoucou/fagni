services:
  - type: web
    name: fagni
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn fagni.wsgi"
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: fagni.settings
      - key: PYTHON_VERSION
        value: 3.11
